export default function AboutMeLayout( {children}) {
    return (
        <section>
          {/* Include shared UI here e.g. a header or sidebar */}
          <nav>NAV</nav>
     
          {children}
        </section>
      )
}